# Портфолио | Тимофей Ходаков

Личное портфолио для демонстрации проектов и работ.

## Структура проекта

```
портфолио/
├── index.html          # Главная страница портфолио
├── assets/             # Папка для статических ресурсов
│   ├── style.css       # Основные стили (CSS)
│   └── main.js         # Основной JavaScript файл
├── data/               # Папка для данных
│   └── projects.json   # JSON файл с данными о проектах
└── README.md           # Этот файл с описанием
```

## Описание папок и файлов

### `index.html`
Главная страница портфолио. Содержит:
- Header с именем и должностью
- Секцию с проектами в виде карточек
- Footer с копирайтом

### `assets/`
Папка для всех статических ресурсов (стили, скрипты, изображения).

### `assets/style.css`
Файл со стилями (CSS). Определяет:
- Цветовую схему (тёмная тема)
- Стили для hero-секции
- Стили для карточек проектов
- Адаптивность для мобильных устройств
- Анимации при наведении

### `assets/main.js`
Основной JavaScript файл. Содержит:
- Массив `projects` с данными о проектах
- Функцию `renderProjects()` для отображения проектов
- Функцию `addProject()` для добавления новых проектов
- Функцию `loadProjects()` для загрузки из localStorage
- Логику для кнопки добавления проекта

### `data/`
Папка для хранения данных проекта.

### `data/projects.json`
JSON файл с массивом проектов. Каждый проект содержит:
- `id` - уникальный идентификатор
- `name` - название проекта
- `description` - описание
- `url` - ссылка на проект
- `icon` - эмодзи-иконка
- `tags` - массив тегов
- `date` - дата добавления

## Как добавить новый проект

### Способ 1: Через JavaScript
Отредактируйте массив `projects` в файле `assets/main.js`:

```
javascript
{
    id: 4,
    name: 'Название проекта',
    description: 'Описание проекта',
    url: 'https://ссылка.na.proekt',
    icon: '🎯'
}
```

### Способ 2: Через JSON
Добавьте новый объект в массив в файле `data/projects.json`.

## Как открыть портфолио

Просто откройте файл `index.html` в любом браузере.

## Технологии

- HTML5
- CSS3
- JavaScript (ES6+)
- JSON для хранения данных
- LocalStorage для сохранения изменений
